{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "build C++",
            "command": "g++",
            "args": [
                "${file}",
                "-std=c++17",
                "-O2",
                "-g",
                "-o",
                "a.out"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "컴파일러: /usr/bin/g++"
        }
    ]
}